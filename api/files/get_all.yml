info:
  name: Get All Files
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/cases/records/{{case_id}}/files"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
