info:
  name: Get All Files
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/{{store_name}}/records/{{case_id}}/files"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("list files requires valid auth", function () {
          expect(res.status).to.not.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
