info:
  name: Get One File
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/cases/records/{{case_id}}/files/{{test_filename}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
