info:
  name: Get One File
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/{{store_name}}/records/{{case_id}}/files/{{test_filename}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("download file returns 200", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
