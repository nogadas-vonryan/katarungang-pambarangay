info:
  name: Delete File
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/v1/{{store_name}}/records/{{case_id}}/files/{{test_filename}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("delete file returns no content", function () {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
