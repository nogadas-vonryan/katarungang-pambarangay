info:
  name: Rename File
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/v1/cases/records/{{case_id}}/files/{{test_filename}}"
  body:
    type: json
    data: |-
      {
        "name": "{{renamed_test_filename}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
