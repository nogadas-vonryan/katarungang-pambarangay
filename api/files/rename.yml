info:
  name: Rename File
  type: http
  seq: 5

http:
  method: PATCH
  url: http://{{base_url}}/v1/cases/records/{{case_id}}/files/{{test_filename}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
