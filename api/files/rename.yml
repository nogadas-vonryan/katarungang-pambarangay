info:
  name: Rename File
  type: http
  seq: 5

http:
  method: PATCH
  url: http://{{base_url}}/v1/cases/records/case-007-26/files/caffeine.png
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
