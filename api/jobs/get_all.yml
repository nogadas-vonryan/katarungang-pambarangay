info:
  name: Get All Jobs
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/v1/jobs"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("list jobs requires valid auth", function () {
          expect(res.status).to.not.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
