info:
  name: Get All Jobs
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/v1/jobs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
