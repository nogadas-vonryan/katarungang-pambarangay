info:
  name: Cancel Job
  type: http
  seq: 13

http:
  method: POST
  url: "{{base_url}}/v1/jobs/{{job_id}}/cancel"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
