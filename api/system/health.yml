info:
  name: Check API Health
  type: http
  seq: 1

http:
  method: GET
  url: http://{{base_url}}/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
