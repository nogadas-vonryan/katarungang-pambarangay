info:
  name: Get Setup Status
  type: http
  seq: 8

http:
  method: GET
  url: http://{{base_url}}/setup/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
