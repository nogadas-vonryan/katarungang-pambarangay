info:
  name: Get Setup Status
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/setup/status"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("setup status endpoint is reachable", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
