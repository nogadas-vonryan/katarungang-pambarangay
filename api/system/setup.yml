info:
  name: Complete Setup
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/setup/complete"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
