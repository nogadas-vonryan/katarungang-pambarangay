info:
  name: Logout
  type: http
  seq: 6

http:
  method: POST
  url: http://{{base_url}}/auth/logout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
