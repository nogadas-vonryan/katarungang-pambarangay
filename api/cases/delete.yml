info:
  name: Delete Case Record
  type: http
  seq: 29

http:
  method: DELETE
  url: "{{base_url}}/v1/{{store_name}}/records/{{case_id}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("delete case returns no content", function () {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
