info:
  name: Put Case Record
  type: http
  seq: 25

http:
  method: PUT
  url: http://{{base_url}}/v1/cases/records/case-001-26
  body:
    type: json
    data: |-
      {
        "WOOOOO": "HAAAAAxAA"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
