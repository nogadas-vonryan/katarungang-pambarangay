info:
  name: Get All Case Records
  type: http
  seq: 19

http:
  method: GET
  url: "{{base_url}}/v1/cases/records"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
