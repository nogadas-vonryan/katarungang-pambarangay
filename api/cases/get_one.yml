info:
  name: Get One Case Record
  type: http
  seq: 23

http:
  method: GET
  url: http://{{base_url}}/v1/cases/records/case-001-26
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
