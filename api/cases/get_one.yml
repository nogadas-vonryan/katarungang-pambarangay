info:
  name: Get One Case Record
  type: http
  seq: 23

http:
  method: GET
  url: "{{base_url}}/v1/cases/records/{{case_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
