info:
  name: Get All Backups
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/v1/backups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
