info:
  name: Restore Backup
  type: http
  seq: 3

http:
  method: POST
  url: http://{{base_url}}/v1/backups/{{backup_name}}/restore
  body:
    type: json
    data: |-
      {
        "targetStore": "cases",
        "mode": "overwrite",
        "dryRun": true,
        "force": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
