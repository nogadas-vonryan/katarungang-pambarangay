info:
  name: Create Backup
  type: http
  seq: 2

http:
  method: POST
  url: http://{{base_url}}/v1/backups?scope=cases
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
