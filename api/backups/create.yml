info:
  name: Create Backup
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/v1/backups?scope={{backup_scope}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
