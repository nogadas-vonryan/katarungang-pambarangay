info:
  name: Get All Stores
  type: http
  seq: 2

http:
  method: GET
  url: http://{{base_url}}/v1/stores
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
